#!/bin/sh
. "$(dirname -- "$0")/_/husky.sh"

# Run the version update script
node scripts/update-version.js

# Stage the changes
git add package.json

# Commit the changes
git commit -m "chore: update version"

# Exit with status of version update script
exit $? 